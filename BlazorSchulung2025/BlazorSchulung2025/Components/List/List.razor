@foreach (var item in Items)
{
    <ListItemRenderer Item="item"></ListItemRenderer>
}

@code {
    [Parameter]
    public IEnumerable<IListItem> Items { get; set; } = [];

    [Parameter]
    public EventCallback<IEnumerable<IListItem>> ItemsChanged { get; set; }
}
