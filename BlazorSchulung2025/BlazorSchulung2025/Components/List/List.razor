@foreach (var item in Items)
{
    <ListItemRenderer Item="item"></ListItemRenderer>
}

@code {
    [Parameter]
    public List<IListItem> Items { get; set; } = [];

    [Parameter]
    public EventCallback<List<IListItem>> ItemsChanged { get; set; }
}
